# Master Index — Every Vault, One Catalog

*This is the **singular interface** over the whole brain: one line per page across every non-private context-vault. The assistant reads this first when answering, then opens the specific pages it needs — in whichever vault they live. The nightly sweep and the Phase 4 ontology refresh keep it current. Private vaults are deliberately omitted.*

*Until Phase 4 builds out the ontology, this catalog simply lists what the sweep has filed, grouped by vault.*

<!-- VAULTS:START (the sweep/ontology fills one section per non-private vault) -->
## {{context-a}}

_(empty for now)_

## {{personal}}

_(empty for now)_
<!-- VAULTS:END -->

---

*Entry format: `- [[Page Title]] — vault: <context> — one-line summary. (N sources)`*
